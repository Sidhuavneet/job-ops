/** @type {import('next').NextConfig} */
const nextConfig = {
  // The UI reads/writes career-ops data files in the parent directory.
  // Nothing leaves the repo — everything stays inside temp/career-ops.
};

export default nextConfig;
